# 2026-05-15 — Phase 0 Complete, Phase 1 Started

## Phase 0 — Bootstrap ✅

- Repo created: `viet-commits/ringsidesports-next`
- Monorepo scaffolded with pnpm workspaces + Turborepo
- All checks pass: typecheck, lint, test, build
- Tagged `v0.0.0`

## Phase 1 — Backend Foundation 🚧

- Docker 29 installed on metalduet (45.124.55.87)
- Docker Compose stack defined (postgres:16, redis:7, meilisearch, medusa-backend)
- Ports offset to avoid cPanel conflicts (5433, 6380, 7700, 9000)
- Medusa v2 config scaffolded with AUD/en-AU defaults
- Cloudflare credentials provisioned (token: cfut_8AUE...)
- Sub-agent deploying infra + DNS records

## Decisions

- **No Hetzner** — everything on metalduet VPS (Viet's directive)
- **No Caddy on 80/443** — nginx/cPanel already occupies those; proxy via nginx includes
- **No Backblaze B2** — backups to Cloudflare R2

## Blockers

- GitHub token needs `workflow` scope for CI (Viet aware)
- 1Password not provisioned yet

## Open

- Cloudflare zone API access appears permission-limited — may need token scope adjustment
- 93% disk usage on /dev/vda1 (30 GB free) — may need cleanup before Postgres grows

## Phase 4 — MeiliSearch Integration ✅ (2026-05-15 12:12)

- **Branch:** `phase-4-search`
- **MeiliSearch:** v1.13 running on 45.124.55.87:7700
- **Index:** `products` created with 483 published products

### What was built

| File | Purpose |
|------|---------|
| `apps/backend/src/search/meilisearch-client.ts` | MeiliSearch client wrapper for backend |
| `apps/backend/src/search/index-products.ts` | TypeScript product indexing script (reads catalog JSON, configures index, batches docs) |
| `apps/storefront/lib/meilisearch.ts` | MeiliSearch client for storefront (public key) |
| `apps/storefront/app/api/search/route.ts` | Next.js API route: `GET /api/search?q=&page=&limit=` with facets |
| `scripts/index-meilisearch.mjs` | Standalone Node.js indexing script (zero dependencies) |

### Index configuration
- **searchableAttributes:** title, description, categories, tags, variants.title
- **filterableAttributes:** categories, tags, price, stockStatus, productType
- **sortableAttributes:** price, title
- **typoTolerance:** enabled

### Verification
- 483 products indexed in 5 batches
- "gloves" → 146 hits with facet distribution
- Typo tolerance: "glovs" → matches "gloves" products
- Filter: `stockStatus = in_stock` works
- Both package.json files updated with `meilisearch: ^0.47.0`

## Phase 5 — Storefront Build ✅ (2026-05-15 12:38)

- **Branch:** `phase-5-storefront`
- **Design:** Tailwind CSS v4 + shadcn/ui patterns, rebuilt from scratch (not pixel-matching Flatsome)
- **Dependencies added:** `@tailwindcss/postcss`, `lucide-react`

### Architecture

- Static export (`output: "export"`) for Cloudflare Pages
- Mock product data (12 products, 6 categories) for build-time rendering
- Client-side cart state via React Context (persisted to localStorage)
- `"use client"` only on interactive components
- `color-scheme: light` enforced via `viewport` export on all pages

### Files Created

**Tailwind Config:**
| File | Purpose |
|------|---------|
| `postcss.config.mjs` | PostCSS with `@tailwindcss/postcss` |
| `app/globals.css` | Tailwind v4 imports + design tokens (--color-primary, --color-background, etc.) |

**UI Components (shadcn/ui pattern):**
| File | Purpose |
|------|---------|
| `components/ui/button.tsx` | Button with 5 variants (primary/secondary/outline/ghost/accent), 3 sizes |
| `components/ui/input.tsx` | Form input with label and error support |
| `components/ui/badge.tsx` | Badge with 5 variants (default/success/danger/warning/outline) |
| `components/ui/card.tsx` | Card with header/content/footer sub-components |
| `components/ui/dialog.tsx` | Modal dialog with backdrop blur and close button |

**Product Components:**
| File | Purpose |
|------|---------|
| `components/product/product-card.tsx` | Product card with image, title, price (AUD), stock badge, hover effects |
| `components/product/product-grid.tsx` | Responsive grid wrapper (2/2/3/4 columns) |
| `components/product/variant-selector.tsx` | Size/colour picker with stock indicator and low-stock badges |

**Layout Components:**
| File | Purpose |
|------|---------|
| `components/layout/header.tsx` | Dark sticky header with logo, category nav, search icon, cart icon with count |
| `components/layout/footer.tsx` | Footer with shop/info links, contact details, payment methods, copyright |
| `components/cart/cart-drawer.tsx` | Slide-out cart drawer with quantity controls, GST breakdown, checkout btn |

**Search:**
| File | Purpose |
|------|---------|
| `components/search/search-bar.tsx` | Search input with submit button, focus ring, compact mode |

**Lib Utilities:**
| File | Purpose |
|------|---------|
| `lib/format.ts` | Price formatting (AUD), GST calculation, ex-GST amount |
| `lib/products.ts` | Mock product data (12 products, 6 categories), filter/query helpers |
| `lib/cart.tsx` | Cart context with reducer, localStorage persistence, GST-aware totals |

**Pages:**
| Route | Description |
|-------|-------------|
| `/` | Hero section, featured categories grid, top sellers, trust bar |
| `/products` | Product listing with faceted sidebar, search, sort, pagination, mobile filter drawer |
| `/products/[handle]` | Product detail with image gallery, variant selector, add to cart, SKU/tags |
| `/cart` | Full cart page with quantity controls, order summary, GST breakdown |
| `/search` | Search results with text matching, faceted filters, pagination |

### Verification

- ✅ `pnpm typecheck` — passes (storefront only; shared-types has pre-existing issue)
- ✅ `pnpm lint` — passes with 0 warnings
- ✅ `pnpm build` — static export succeeds (19 pages generated)
- ✅ `pnpm test` — existing bootstrap test passes
- ✅ API route `/api/search` removed (incompatible with static export)
- ✅ ESLint config updated to ignore `.next/` build output

### Notes

- Search uses client-side text matching against mock data (no MeiliSearch in static export)
- Cart persists to localStorage between sessions
- All prices displayed in AUD with $ prefix and GST note
- Mobile responsive: hamburger nav, filter drawer, touch-friendly controls
- `color-scheme: light` on all pages fixes §2.5 iOS dark mode bug

## Phase 6 — Redirect Map + SEO ✅ (2026-05-15 18:34)

- **Branch:** `phase-6-redirects`

### What was built

| File | Purpose |
|------|---------|
| `scripts/generate-redirect-map.mjs` | Queries WP DB → generates 301 redirect map (1,033 entries) |
| `scripts/generate-sitemap.mjs` | Generates sitemap.xml from catalog data (517 URLs) |
| `scripts/extract-seo-metadata.mjs` | Extracts Yoast SEO metadata from WP DB |
| `infra/workers/redirect-worker/src/index.ts` | Cloudflare Worker for 301 redirect handling via KV |
| `infra/workers/redirect-worker/wrangler.toml` | Worker config with KV namespace + routes |
| `infra/workers/redirect-worker/tsconfig.json` | TypeScript config for worker |
| `apps/storefront/public/robots.txt` | Robots.txt pointing to sitemap |
| `apps/storefront/app/layout.tsx` | Updated with JSON-LD (Organization + WebSite), Twitter card, OG image |

### Redirect Map

- **1,033 total redirects** (all 301)
  - 483 product redirects: `/product/{slug}/` → `/products/{new-handle}`
  - 97 category redirects: `/product-category/{slug}/` → `/products?category={slug}`
  - 422 tag redirects: `/product-tag/{slug}/` → `/products?tag={slug}`
  - 32 static page redirects (about, contact, blog, FAQ, etc.)
  - WP content paths → `/` (wp-content, wp-admin, wp-json, feed, comments)
  - Account pages → `/cart` (my-account, checkout, order-history, wishlist)
- 8 products skipped (missing data in WP DB)
- Output: `/tmp/redirect-map.json` (584 KB)

### Sitemap

- **517 URLs** in single sitemap (well under 50K limit)
  - 1 homepage (priority 1.0)
  - 3 static pages (products, cart, search)
  - 30 category pages
  - 483 product pages (priority 0.6)
- Proper `<lastmod>`, `<changefreq>`, `<priority>` values
- Output: `/tmp/sitemap.xml` (114 KB)

### SEO Metadata

- **11 posts with Yoast SEO metadata** (3 pages, 7 blog posts, 1 unknown)
  - 8 with Yoast title, 9 with meta description, 9 with focus keyword
  - Supplier products (auto-imported) have no individual Yoast data — expected
- Site-wide settings exported: `wpseo`, `wpseo_social`, `wpseo_titles`
- No category/tag term-level SEO metadata found
- Output: `/tmp/seo-metadata.json` (29 KB)

### OpenGraph & Structured Data

- **OG metadata:** og:title, og:description, og:image (1200×630), og:url, site_name, locale
- **Twitter card:** summary_large_image with title, description, image
- **JSON-LD:** Organization (name, url, logo, contact, address) + WebSite (SearchAction)

### Verification

- ✅ `pnpm --filter storefront typecheck` — passes
- ✅ `pnpm --filter storefront build` — static export succeeds (19 pages)
- ✅ Redirect map: 1,033 unique entries, all 301, no duplicate sources
- ✅ Sitemap: valid XML, 517 URLs
- ✅ Layout: no regression on existing functionality

### Notes

- Redirect worker uses Cloudflare KV for the redirect map; bulk upload via `wrangler kv:bulk put`
- KV namespace ID is placeholder — needs provisioning when worker is deployed
- The worker preserves query strings on redirect (e.g., `?category=boxing` survives the 301)
