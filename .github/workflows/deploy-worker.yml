name: Deploy Sync Worker

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "apps/sync/**"
      - "packages/shared-types/**"

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Placeholder — deploy sync in phase 3"
